export const BOT_DISPLAY_NAME = "88";
export const ADMIN_ROLE_NAME = "88 Admin";
export const MAX_RUST_SERVERS_PER_GUILD = 10;

/** Discord embed color for all bot embeds (FFFFCC). */
export const EMBED_COLOR = 0xffffcc;

/** In-game quick-chat emote that triggers a free kit (exact string after player name). */
export const EMOTE_TRIGGER_WOOD = "d11_quick_chat_i_need_phrase_format wood";

/** Kit given when a player uses {@link EMOTE_TRIGGER_WOOD}. */
export const FREE_KIT_NAME = "FREEkit1";

/** Min seconds between kit grants per player per server (spam guard). */
export const EMOTE_KIT_COOLDOWN_SEC = 30;
